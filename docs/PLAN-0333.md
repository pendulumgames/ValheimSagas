# 0.3.33 — Recorded adventures and reliable loading

Approved scope (September 23, 2026): implement the previous improvements/features except host diagnostics download, earned epithets (including in-game titles), and Steam achievements.

## Preserve existing map rendering
The 0.3.30 overview/detail renderer and pin scalability work are already implemented and tested. Do not replace it. Live dedicated website inspection found stale 0.3.27 frontend files; a complete package update is an operational prerequisite. Remaining work is independent loading and efficient backend queries, not another renderer rewrite.

## Implementation checklist
- [x] Display executing website and server versions; warn about stale/mixed web files.
- [x] Bootstrap world identity/presence/map independently of expensive statistics. Keep last known map on transient failures, retain privacy/world reset rules.
- [x] Cache bounded retained history with write/retention invalidation; reduce repeated coordinate lookup work while preserving complete filtered counts and fresh sharing permissions.
- [x] Compact expandable connection status: loading exploration, delayed statistics, disconnected, last successful update.
- [x] Adventure recap using a browser-local per-world last-visit bookmark (first visit explicitly last24h). Show factual highlights and new saga chapters; do not auto-mark unread updates continuously.
- [x] Boss trophy hall: known boss sequence, first recorded victory/team/time, fastest eligible measured victory. No invented completion of earlier bosses, solo credit or unknown multi-phase times.
- [x] Compare two consenting Vikings with existing time filters; clear finishing-blow vs participation and collection labels.
- [x] Saga-linked map moments from recorded event coordinates only, respecting current map/profile sharing and explored ground. No coordinates inferred from story prose.
- [x] Investigate optional Valheim Moments integration. Ship only a safe supported integration; document exact blocker if current export lacks identity/consent. Never fake attached media or auto-capture.
- [x] Backend/privacy/regression/browser checks; stress recheck as appropriate; package0.3.33, README/changelog/STATUS update and GitHub sync.

## Layout decisions
Keep World map first. Place a compact recap below Online Vikings. Add Trophy hall and Compare Vikings as views inside the existing Hall of renown, sharing its time filters. Place chapter map links beside recorded evidence. Keep connection/version details collapsed except actionable mismatch warnings.

## Constraints
No live server, Deep North or save changes. Moments repository changes are limited to the requested integration plan; no code edits. No paid API calls. Isolated synthetic tests clearly labeled; never claim dedicated/in-game verification from browser tests. Gale Test replacement only with game closed; skip Jewelcrafting. No mod-site publication.

## Current Moments finding
Private binary gallery contains animated WebP clips and raw preview images but no persistent world/character identifiers or public versioned export callback. Safe automatic association requires capture-time identity plus explicit sharing consent; the required contract is documented in C:/repository/ValheimMoments/SAGAS-INTEGRATION-PLAN.md. This does not block other features.
