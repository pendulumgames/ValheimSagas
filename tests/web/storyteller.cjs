const {chromium}=require('playwright'),assert=require('node:assert/strict'),fs=require('fs'),path=require('path');
const url=process.env.SAGAS_TEST_URL||'http://127.0.0.1:9847/';if(!/^http:\/\/(127\.0\.0\.1|localhost):\d+\/$/.test(url))throw Error('Loopback fixture required');
(async()=>{const f=JSON.parse(fs.readFileSync(process.env.SAGAS_LOGIN_FIXTURE||path.resolve(__dirname,'../../.dev/fixture/synthetic-logins.json')));const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage({viewport:{width:390,height:844}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.addInitScript(t=>sessionStorage.setItem('sagas.viewer',t),f.champion);await p.goto(url);await p.waitForFunction(()=>state.data&&!state.busy&&loginSession?.playerId);await p.locator('#accountButton').click();await p.locator('#sagaSettingsButton').click();await p.waitForFunction(()=>personalLore!==null);assert(await p.locator('#loreSettings').isVisible());assert(await p.locator('#personalLoreFields').isHidden());await p.locator('#loreModePersonal').check();await p.locator('#loreAdvanced summary').click();await p.locator('#lorePresetChoice').selectOption('__new');await p.locator('#lorePresetName').fill('Synthetic browser preset');await p.locator('#lorePresetRoute').fill('@preset/synthetic-test');await p.locator('#personalLoreKey').fill('synthetic-browser-key-never-sent');await p.locator('#loreSettings button[type=submit]').click();await p.waitForFunction(()=>personalLore?.hasKey&&personalLore.active==='Synthetic browser preset');assert.equal(await p.locator('#personalLoreKey').inputValue(),'');assert(!(await p.locator('#loreSettings').textContent()).includes('synthetic-browser-key-never-sent'));await p.locator('#lorePresetRoute').fill('vendor/paid');await p.locator('#loreSettings button[type=submit]').click();await p.waitForFunction(()=>document.querySelector('#loreSaveStatus').textContent.includes('enable paid credits'));await p.locator('#lorePaid').check();await p.locator('#lorePrice').fill('1.25');await p.locator('#loreRequests').fill('2');await p.locator('#loreSettings button[type=submit]').click();await p.waitForFunction(()=>personalLore?.presets.some(x=>x.route==='vendor/paid'&&x.allowPaid));assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true);await p.screenshot({path:'.dev/qa/storyteller-mobile.png'});await p.locator('#removeLorePreset').click();await p.waitForFunction(()=>personalLore?.active===''&&!personalLore.presets.some(x=>x.name==='Synthetic browser preset'));await p.locator('#loreModePersonal').check();await p.locator('#removeLoreKey').click();await p.waitForFunction(()=>personalLore?.hasKey===false);await p.locator('#closeSagaSettings').click();
const fog=await p.evaluate(()=>{const c=document.createElement('canvas');c.width=256;c.height=256;const g=c.getContext('2d');view.width=view.height=256;view.x=view.z=0;view.scale=1;drawFog(g);const first=g.getImageData(0,0,256,256).data;drawFog(g);const next=g.getImageData(0,0,256,256).data;let brown=true,same=true,values=new Set();for(let i=0;i<first.length;i+=4){brown&&=first[i]>first[i+1]&&first[i+1]>first[i+2]&&first[i+3]===255;same&&=first[i]===next[i];values.add(first[i]);}return {brown,same,variation:values.size};});assert(fog.brown&&fog.same&&fog.variation>10,'Fog is textured brown, opaque and stable across redraws');assert.deepEqual(errors,[]);console.log('PASS personal storyteller browser presets, paid opt-in rejection/acceptance, hidden key, removal, mobile layout and stable opaque brown fog; simulated provider only.');}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});
